<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lmdestiny.dao.FundDao">

<insert id="saveFund" parameterType="com.lmdestiny.model.Fund">
insert into fund(id,orgId,orgName,orgNo,orgCode,yearly,preFund,businessFund,dutyFund,buildFund)
values(#{id},#{organization.id},#{orgName},#{orgNo},#{orgCode},#{yearly},#{preFund},#{businessFund},#{dutyFund},#{buildFund})
</insert>
</mapper>